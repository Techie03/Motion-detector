we need open-cv,pandas,datetime.
command prompt code:

pip install pip
pip install opencv-python
pip install pandas
pip install datetime

A screen shot of the command prompt has been kept in the wiki section.
